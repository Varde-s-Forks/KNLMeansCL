name: Build for Linux

on:
  workflow_call:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runs-on: ubuntu-latest
          - arch: aarch64
            runs-on: ubuntu-24.04-arm

    runs-on: ${{ matrix.runs-on }}
    steps:
      - uses: actions/checkout@v6

      - name: Build wheels
        uses: pypa/cibuildwheel@v3.4
        env:
          CIBW_BUILD: cp312-*
          CIBW_ARCHS_LINUX: ${{ matrix.arch }}
          CIBW_BEFORE_ALL_LINUX: >
            dnf install -y epel-release &&
            dnf --enablerepo=powertools install -y ocl-icd-devel boost-devel pkgconf-pkg-config
          CIBW_ENVIRONMENT: MESON_ARGS="-Denable_avs=false -Dstatic=true"
          CIBW_SKIP: "*-musllinux*"
          CIBW_TEST_REQUIRES: pytest vapoursynth
          CIBW_TEST_COMMAND: pytest -v {project}/tests
          CIBW_BEFORE_TEST_LINUX: dnf install -y epel-release && dnf install -y pocl

      - name: Upload VapourSynth plugin
        uses: actions/upload-artifact@v7
        with:
          name: knlmeanscl-vs-linux-${{ matrix.arch }}
          path: ./wheelhouse/*.whl
